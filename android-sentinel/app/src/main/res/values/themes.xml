<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; the actual UI colors come from Compose Material 3 dynamic/dark theme.
         This is just the launch theme applied before Compose takes over. -->
    <style name="Theme.Sentinel" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
